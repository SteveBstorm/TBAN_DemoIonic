import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'TBAN_IonicDemo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
